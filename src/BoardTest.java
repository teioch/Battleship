/**
 * Created with IntelliJ IDEA.
 * User: Geir
 * Date: 3/4/14
 * Time: 16:53
 * To change this template use File | Settings | File Templates.
 */


/*public class BoardTest {
    @Test
    public void shouldBeLegalHorizontal(){
        Board board = new Board();
        Ship ship = new Ship(Type.CORVETTE);
        int x = 3;
        int y = 3;
        boolean horizontal = true;
        assertTrue(board.attemptPlacement(x, y, horizontal, ship.getType().getSize()));
    }

    @Test
    public void shouldBeLegalVertical(){
        Board board = new Board();
        Ship ship = new Ship(Type.CORVETTE);
        int x = 3;
        int y = 3;
        boolean horizontal = false;
        assertTrue(board.isLegal(x,y,horizontal, ship.getType().getSize()));
    }

    @Test
    public void shouldNotBeLegalHorizontal(){
        Board board = new Board();
        Ship ship = new Ship(Type.BATTLESHIP);
        int x = 7;
        int y = 7;
        boolean horizontal = true;
        assertFalse(board.isLegal(x, y, horizontal, ship.getType().getSize()));
    }
}
*/